# Buat Laporan di Sini

Poin-poin perubahan yang dilakukan pada repository berikut:
    1. Nama file output dalam MakeFile yang semula adalah **myapp** menjadi **MyApp**
    2. Menambahkan variabel untuk menyimpan input string dari user dengan nama **user_input**
    3. Mengubah output program yang semula adalah **Hello World!** menjadi **Hello <user_input>**
    4. Ditemukan error karena direktori untuk **obj** tidak terdapat dalam repository
    5. Menambahkan direktori **obj** dalam untuk objek eksekusi MakeFile
    6. Program berhasil diesksekusi dengan output yang sesuai
